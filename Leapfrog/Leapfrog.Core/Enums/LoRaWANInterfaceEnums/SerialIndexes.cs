namespace Leapfrog.Core.Enums.LoRaWANInterfaceEnums
{
    public enum SerialIndexes
    {
        INDEX_ID,    // The ID of the packet
        INDEX_CMD,  // The type of packet
        INDEX_LEN,  // The length of the packet
        INDEX_DATA  // The first byte of data in the packet
    }
}
