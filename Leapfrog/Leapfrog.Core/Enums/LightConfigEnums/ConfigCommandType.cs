namespace Leapfrog.Core.Enums.LightConfigEnums
{
    public enum ConfigCommandType
    {
        CONFIG_GET = 0x40,      // Retrieve configuration from a device
        CONFIG_SET = 0x41,      // Assign configuration to a device
    }
}
